# 암묵적 타입 변환

## 이 폴더의 목적

자바스크립트의 동적 타입 특성으로 인한 의도하지 않은 타입 변환 문제와 예방법을 코드로 확인한다.

## 발생 가능한 문제

- 숫자 연산에서 문자열이 섞이면 문자열 결합이 발생함
- 조건문에서 falsy 값으로 인해 로직이 의도와 다르게 동작함

## 예방 방법

- 연산 전에 타입을 명시적으로 변환한다.
- 조건식에서 값의 의미를 명확히 검사한다.

## 한 문장 정리

암묵적 타입 변환은 명시적 타입 처리와 규칙으로 관리해야 한다.
